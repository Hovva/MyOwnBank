meta:
  environment: dotnet
  toolchain:
    name: dotnet
    version: "10"

build:
  additionalCommands: dotnet publish src/MyOwnBank.Bot/MyOwnBank.Bot.csproj -c Release -o ./publish

run:
  persistenceMount: /data
  containerPort: 8080
  command: dotnet MyOwnBank.Bot.dll
